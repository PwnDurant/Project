spring:
  application:
    name: GoBang

  datasource:
    url: jdbc:mysql://127.0.0.1:3306/java_gobang?characterEncoding=utf8&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: zqq13127837702
    driver-class-name: com.mysql.cj.jdbc.Driver

logging:
  file:
    name: GoBang.log

mybatis:
  mapper-locations: classpath:mapper/**Mapper.xml

